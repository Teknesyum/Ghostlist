---
id: T12
title: Güncelleme bildirimi + winget manifest + test kapsamı
rol: usta
model: sonnet
depends: [T8]
owns: [Ghostlist.Core/Updates/, packaging/winget/, Ghostlist.Tests/ViewModelTests/, Ghostlist.Tests/UpdateTests/]
yan_etki: [Ghostlist.App/ViewModels/MainViewModel.cs, Ghostlist.App/Localization/tr.json, Ghostlist.App/Localization/en.json, .github/workflows/release.yml, README.md]
status: done
tur: 1
agent_id: tur2-tek-ajan
---
## Amaç
Üç kapanış işi: ürünün yeni sürümden haberi olsun, dağıtımın `irm | iex` dışında saygın
bir yolu olsun, ve arayüz katmanının testi olsun (bugün 153 testin hepsi Core'da —
ViewModel'ler hiç test edilmiyor).

## Kabul kriteri
1. **Güncelleme kontrolü.** GitHub Releases API'sinden en son sürüm etiketi okunur,
   mevcut sürümle karşılaştırılır. Yeni sürüm varsa arayüzde **sessiz bir bildirim şeridi**
   + sürüm notlarına bağlantı. İndirme ve kurma **yok**.
2. **Kontrol kullanıcının denetiminde.** Açılışta otomatik kontrol **varsayılan olarak
   kapalı**; ayarlardan açılır, ayrıca elle "Güncellemeleri denetle" düğmesi var.
   Kapalıyken hiçbir ağ çağrısı yapılmaz — bunu testle sabitle.
3. **Ağ hatası sessiz.** Zaman aşımı (5 sn), 403 (API limiti), çevrimdışı — hiçbiri hata
   diyaloğu açmaz, uygulamayı yavaşlatmaz, taramayı engellemez. Elle kontrolde sonuç
   gösterilir, otomatik kontrolde sessizce geçilir.
4. **winget manifest.** `packaging/winget/` altında üç dosyalı manifest
   (`.installer.yaml`, `.locale.en-US.yaml`, `.yaml`), `Teknesyum.Ghostlist` kimliğiyle,
   sürüm ve SHA256 alanları `Directory.Build.props` ve release çıktısıyla uyumlu.
   **Manifest'i winget deposuna gönderme** — dosyalar depoda hazır dursun, gönderim
   kullanıcının kararı. README'ye winget kurulum satırı eklenir, "yayına alındığında"
   notuyla.
5. **ViewModel testleri.** En az şunlar: dil geçişi `tr`↔`en` anahtarları çözüyor mu;
   kategori gruplaması doğru sayıyor mu; toplu düzeltme seçimi artık klasör ve MSIX'i
   gerçekten dışarıda bırakıyor mu; güven eşiği altındaki bulgu düzeltilebilir görünmüyor mu.
   Bu dördü ürünün güvenlik vaadinin arayüz tarafı — testsiz kalmamalı.
6. **Dil dosyası bütünlüğü testi.** `tr.json` ve `en.json` anahtar kümeleri birebir aynı;
   biri eksikse test **kırmızı** olur. Bugün bu elle kontrol ediliyor.
7. `dotnet build -c Release` yeşil, tüm testler yeşil.

## Bağlam
- T8 `Directory.Build.props` ile sürümü tek yere aldı; güncelleme kontrolü ve winget
  manifest sürümü **oradan** okumalı, sabit yazmamalı.
- Sürüm karşılaştırması `v` önekli etiketlerle çalışır (`v2.0.0`); `SemanticVersion`
  benzeri bir karşılaştırma yaz, dize karşılaştırması yapma (`v2.0.10` < `v2.0.9` tuzağı).
- GitHub API kimliksiz saatte 60 istek verir; bu yüzden otomatik kontrol günde bir kezden
  fazla çalışmasın, son kontrol zamanı `settings.json`'a yazılsın.
- ViewModel testleri için WPF `Dispatcher` bağımlılığı sorun çıkarabilir; ViewModel'ler
  T5'te `ObservableObject` üstüne kuruldu, UI thread'i gerektirmeyen yollarını test et.
  Gerektiriyorsa testi zorlamak yerine ViewModel'i test edilebilir hale getir.
- README `yan_etki`: yalnızca winget satırı ekle, yeniden yazma.

## Kayıt noktası
Yedi kriterin yedisi karşılandı. `dotnet build Ghostlist.sln -c Release` 0 uyarı 0 hata,
`dotnet test Ghostlist.sln -c Release` 283 test yeşil (Ghostlist.Tests 262 + Ghostlist.AppTests 21).

## Çıktı
**Ne yapıldı.** `Ghostlist.Core/Updates/` altında üç dosya: `SemanticVersion` (v önekini ve
`+build` ekini atar, 1–4 parçalı sürümü çözer, ön sürümü final'in altına koyar, karşılaştırma
sayısal), `UpdateChecker` (durum anahtarları `disabled/too_soon/up_to_date/available/unreachable`,
otomatik kontrol günde bir, son kontrol zamanı ayarlara yazılır, ağ hatası `unreachable`'a
yutulur, iptal yutulmaz), `GitHubReleaseSource` (5 sn zaman aşımı, yalnızca `tag_name` ve
`html_url` okunur, indirme/kurma yok). Arayüzde `UpdateBannerViewModel` + üst şeritte sessiz
bildirim bandı, alt bilgide "Güncellemeleri denetle" düğmesi ve otomatik kontrol kutusu.
`packaging/winget/` altında üç dosyalı 1.6.0 manifest + gönderim yönergesi README'si; CI'ya
manifest sürümünü `Directory.Build.props` ile karşılaştıran, uyuşmazsa işi kıran adım eklendi.

**Üç kritik kriterin kod üzerinden doğrulaması.**
1. Otomatik kontrol kapalıyken ağ çağrısı yok: `UpdateChecker.CheckAsync` `manual: false`
   yolunda `source`'a dokunmadan önce `settings.AutomaticUpdateCheck` bakıp döner
   (`UpdateChecker.cs:43-45`). `NoNetworkCallIsMadeWhileAutomaticCheckingIsOff` testi
   `source.Calls == 0`, `LastUpdateCheck == null` ve `Saves == 0` iddia eder — imzayı
   çağırıp sonucu yok saymak bu testi geçemez. Ayarın varsayılanı `false`.
2. Sürüm karşılaştırması semantik: `SemanticVersion.CompareTo` Major→Minor→Patch'i `int`
   olarak karşılaştırır. `ComparisonIsNumericNotLexicographic` testi `v2.0.10 > v2.0.9`
   olduğunu ve aynı çiftin dize karşılaştırmasında **ters** çıktığını birlikte iddia eder.
3. Toplu düzeltme klasör ve MSIX'i dışarıda bırakıyor: `LeftoverFoldersAndMsixNeverEnterTheAutomaticFix`
   her ikisini güven 100 + iki bağımsız kanıtla kurup `IsAutoFixable` false ve
   `service.AutoFixable([...])` boş çıkmasını ister; `TheOtherFourCategoriesDoEnterTheAutomaticFix`
   dört kategorinin aynı koşulda girdiğini ölçer, yani test kuralı değil kategori ayrımını sabitler.

**Sözleşme dışına çıkılan yerler.**
- `owns`'ta `Ghostlist.Tests/ViewModelTests/` yazıyordu; oraya yazılamadı. `Ghostlist.Tests`
  hem `Ghostlist.Core` hem `Ghostlist.Cli` referans alıyor ve `Ghostlist.App`'in derleme adı
  `Ghostlist`, `Ghostlist.Cli`'ninki `ghostlist` — Windows'ta aynı dosya adı, tek çıktı
  klasöründe birbirini eziyor. Ürünün ikili adlarını değiştirmek yerine (kısayol, kurulum
  betikleri ve winget takma adları bu adlara bağlı) ayrı bir `Ghostlist.AppTests` projesi
  açıldı, çözüme eklendi. ViewModel testleri orada.
- `owns` genişletildi: `Ghostlist.App/Settings/AppSettings.cs` (T5'in dosyası, `IUpdateSettings`
  uygulaması için), `Ghostlist.App/ViewModels/UpdateBannerViewModel.cs`, `Ghostlist.App/MainWindow.xaml`,
  `.github/workflows/ci.yml`, `Ghostlist.sln`, `Ghostlist.AppTests/`.
- `yan_etki`'de `.github/workflows/release.yml` vardı; dokunulmadı, gerek olmadı.

**Bilinmesi gerekenler.**
- Kriter 6 (dil dosyası anahtar bütünlüğü) zaten `Ghostlist.Tests/LocalizationKeyTests.cs`
  tarafından karşılanıyordu, yeni test yazılmadı.
- `Ghostlist.Core` artık `Updates/GitHubReleaseSource.cs` içinde bir `HttpClient` barındırıyor.
  T11'in Çıktı'sındaki "Core'da sıfır ağ çağrısı" ifadesi bu yüzden eskidi: rapor ve teşhis
  yolları hâlâ ağa çıkmıyor, ama Core'un tamamı için artık doğru değil.
- winget manifest'inde CLI'nin iç yolu `cli\ghostlist.exe`; `release.yml` CLI'yi `publish/cli`
  altına yayımlıyor. `InstallerSha256` 64 sıfır — gerçek yayın yapılmadığı için hesaplanamadı,
  gönderimden önce doldurulmalı.
- Manifest winget deposuna gönderilmedi, push/tag/release yapılmadı.
