---
id: T7
title: README (EN), kurulum betikleri, yönlendirici CLAUDE.md
rol: kayitci
model: haiku
depends: [T5, T6]
owns: [README.md, scripts/, docs/, CLAUDE.md, Ghostlist.Core/CLAUDE.md, Ghostlist.App/CLAUDE.md, Ghostlist.Cli/CLAUDE.md]
yan_etki: []
status: done
tur: 1
agent_id: T7-sole
---
## Amaç
Dışa açılan yüzü yeni kapsama ve yeni isme uydurmak. README **İngilizce** (depo dokümanı
kuralı), uygulama arayüzü Türkçe varsayılan — bu ikisi çelişki değil, açıkça anlatılmalı.

## Kabul kriteri
1. README tamamen yeniden yazılır, İngilizce. Yapı: ne yaptığı → hangi altı kategoriyi
   taradığı → **güvenlik modeli** (kanıt eşiği, zorunlu yedek, geri alma, asla silinmeyenler)
   → tek satır kurulum → CLI kullanımı → kaynaktan derleme → sürüm → destek/sponsor bloğu.
   Sponsor rozeti ve MIT satırı mevcut biçimde korunur.
2. **İsim ve konumlanma.** "ProgramFixer" yalnızca tek bir satırda geçebilir:
   *formerly ProgramFixer* notu. Başka hiçbir yerde geçmez. `Cleaner / Optimizer / Booster`
   gibi kelimeler kullanılmaz.
3. Arayüz dilinin varsayılan Türkçe olduğu, sol alttaki `TR / EN` düğmesiyle değiştiği yazılır.
   Mümkünse İngilizce arayüzden bir ekran görüntüsü; yoksa mevcut görseli koru, uydurma.
4. `scripts/install.ps1` / `uninstall.ps1` T6'nın release asset adıyla **uyuşur** —
   uyuşmuyorsa düzelt ve Çıktı'da bildir. İndirme URL'i `Teknesyum/Ghostlist`.
   README'deki tek satır kurulum komutu betiğin gerçek yolunu göstersin.
5. `docs/GUVENLIK.md` (Türkçe, proje içi çalışma dokümanı): neyin neden silinmediği,
   kanıt eşikleri, geri yükleme adımları. README bunu referans verir.
6. Yönlendirici `CLAUDE.md`: kök + `Ghostlist.Core` + `Ghostlist.App` + `Ghostlist.Cli`,
   her biri **≤20 satır**. Şablon:
   `C:\Users\Administrator\.claude\plugins\cache\teknesyum\teknesyum\1.3.0\skills\relay\assets\folder-claude.template.md`
7. Sürüm numarası **v2.0.0** — isim, kapsam ve yedek formatı değişti. README, csproj sürümü
   ve release notu tutarlı olsun.

## Bağlam
- Depo dokümanı kuralı: git'e giden README/dokümanlar İngilizce, proje içi çalışma
  dosyaları (`docs/GUVENLIK.md`, sözleşmeler) Türkçe.
- Eski README 91 satır; yapısı iyi, kapsam bölümü ve safety model bölümü genişleyerek kalsın.
- `assets/ornek-bozuk-kaldirma-kaydi.png` hâlâ geçerli bir örnek — kullan.
- T5 ve T6'nın Çıktı bölümlerini oku; gerçek komut adlarını ve bayrakları oradan al, uydurma.

## Kayıt noktası
Yedi kriter de tamam, Release derleme ve 153 test yeşil, commit atıldı.

## Çıktı
- README.md tamamen yeniden yazıldı (İngilizce): ne yaptığı → altı kategori tablosu →
  güvenlik modeli → tek satır kurulum → CLI kullanımı ve çıkış kodları → gereksinimler →
  kaldırma → kaynaktan derleme → v2.0.0 sürüm notu → sponsor rozeti + MIT. "ProgramFixer"
  yalnızca 103. satırda: `Current version: **v2.0.0** (formerly ProgramFixer)`.
  Cleaner / Optimizer / Booster geçmiyor.
- Arayüz dili: varsayılan Türkçe, sol alttaki `TR / EN` ile anında değişiyor — README'de
  yazılı, ayrıca gerçek İngilizce arayüzden ekran görüntüsü eklendi
  (`assets/ghostlist-english-ui.png`, uydurma değil, çalışan uygulamadan alındı).
  Eski `assets/ornek-bozuk-kaldirma-kaydi.png` korundu.
- Kriter 4: `release.yml` `Ghostlist-win-x64.zip` üretiyor, `install.ps1` aynı adı
  indiriyor — uyumlu. Ek düzeltme: README kaldırma bölümü kurulum klasöründeki
  `uninstall.ps1` dosyasını gösteriyordu ama o dosya oraya hiç kopyalanmıyordu;
  `install.ps1` artık `uninstall.ps1`'i de kurulum klasörüne indiriyor ve CLI yolunu
  (cli alt klasöründeki `ghostlist.exe`) yazdırıyor.
- `docs/GUVENLIK.md` (Türkçe) yazıldı: kanıt ilkesi + eşik tablosu, "okunamayan yok
  sayılmaz", asla yapılmayanlar listesi, toplu düzeltme kapsamı ve geri yükleme noktası,
  silme değil taşıma, geri alma adımları (arayüz + CLI), test sınırı. README referans veriyor.
- Dört yönlendirici CLAUDE.md yazıldı: kök 20, Core 20, App 19, Cli 19 satır.
- Sürüm v2.0.0: README, `Ghostlist.App.csproj` ve `Ghostlist.Cli.csproj` tutarlı.
- Owns dışına yazıldı, LOG'a düşüldü: `assets/`, `Ghostlist.App/Ghostlist.App.csproj`,
  `Ghostlist.Cli/Ghostlist.Cli.csproj` — kriter 7 sürüm numarası için zorunluydu.
- Karşılanamayan kriter yok.
- Kapsam dışı not: sürüm numarası üç yerde elle tutuluyor (README + iki csproj);
  ileride `Directory.Build.props` ile tek yerden verilebilir — yapılmadı.
