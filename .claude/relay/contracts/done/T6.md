---
id: T6
title: CLI modu + sistem geri yükleme noktası + CI
rol: usta
model: sonnet
depends: [T4]
owns: [Ghostlist.Cli/, Ghostlist.Core/SystemRestore.cs, .github/workflows/, Ghostlist.sln, Ghostlist.Tests/CliTests/]
yan_etki: []
status: done
tur: 1
agent_id: T6-sole
---
## Amaç
Üç ürünleştirme parçası: betiklenebilir bir CLI (IT yöneticisi kitlesi + kendi entegrasyon
testimiz), toplu işlem öncesi sistem geri yükleme noktası (güven), ve derleme/test/yayın
otomasyonu. Kod imzalama **bu turda yok**; SHA256 yayımlanır.

## Kabul kriteri
1. **`Ghostlist.Cli`** ayrı konsol projesi, `Ghostlist.Core`'a bağlı, WPF'e **bağlı değil**.
   Komutlar:
   - `ghostlist scan [--category <ad>] [--json] [--min-confidence <n>]`
   - `ghostlist fix --id <bulgu-id> [--dry-run]`
   - `ghostlist fix --all [--min-confidence 90] [--dry-run]`
   - `ghostlist restore --list` / `ghostlist restore --backup <yol>`
   `--dry-run` hiçbir şey değiştirmez, ne yapacağını yazar. Çıkış kodları: 0 temiz,
   1 bulgu var, 2 hata. `--json` çıktısı satır satır ayrıştırılabilir olsun.
2. **Onaysız yıkım yok.** CLI'da `--all` varsayılan olarak onay ister; `--yes` ile geçilir.
   `--yes` olmadan boru hattında çalışmaz — bilinçli.
3. **Sistem geri yükleme noktası.** `SystemRestore.TryCreate(string description)`:
   yönetici varsa ve System Restore açıksa nokta oluşturur, aksi halde `false` döner ve
   **işlemi engellemez**. Toplu düzeltmeden önce çağrılır (GUI ve CLI). Windows'un 24 saatlik
   sıklık limitine takılırsa bu bir hata değil, sessiz `false`.
4. **CI.** `.github/workflows/ci.yml`: `windows-latest`, PR ve main push'ta
   `dotnet build` + `dotnet test -c Release`.
   `.github/workflows/release.yml`: `v*` tag'inde self-contained single-file `win-x64`
   publish, zip, SHA256 dosyası, GitHub Release'e ekli. İmzalama adımı yok.
5. `Ghostlist.sln` yeni CLI projesini içerir. Tüm projeler derleniyor, `dotnet test` yeşil.
6. CLI komut ayrıştırma testli (argüman → plan eşlemesi). Gerçek registry'ye yazan test yok.

## Arayüzler
T3/T4'ten: `IIssueProvider`, `Finding`, `Evidence`, `FixResult`, `CleanupService`, `IBackupSink`.
CLI **kendi kopyasını yazmaz** — GUI ile aynı `CleanupService` üzerinden çalışır.
CLI metinleri İngilizce; T5'in çeviri tablosuna bağlanma, CLI ayrı ve İngilizce kalır.

## Bağlam
- Yedek kökü: T1'in `BackupPaths` yardımcısı. Ayarlar `%LOCALAPPDATA%\Ghostlist\settings.json`
  (T5 yazıyor) — CLI ayar dosyasına **yazmasın**, gerekiyorsa okusun.
- Sistem geri yükleme için `SRSetRestorePoint` P/Invoke ya da WMI `SystemRestore` sınıfı;
  hangisini seçersen seç, hata yolu sessiz `false` olsun. İstisna sızdırma.
- Depo `github.com/Teknesyum/Ghostlist`; workflow'larda eski `ProgramFixer` adını kullanma.
- `scripts/install.ps1` release asset adına bağımlı — T1 onu güncelledi, release.yml'in
  ürettiği zip adı onunla **uyuşmalı**. Uyuşmazlık kurulumu kırar, kontrol et.

## Kayıt noktası
6/6 kabul kriteri tamam. Release derleme 0 uyarı, `dotnet test` 153/153 yeşil.
CLI gerçek makinede çalıştırıldı: `scan --json` JSONL üretti, `scan` çıkış 1 / boş sonuç 0,
`fix --all --dry-run` 20 hedef (18 kısayol + 2 görev, artık klasör ve MSIX yok) çıkış 1,
`fix --all` boru hattında `--yes` olmadan çıkış 2, `restore --list` 14 yedek listeledi,
bilinmeyen seçenek ve bilinmeyen id çıkış 2. `SystemRestore.TryCreate` gerçekten
`True` döndürdü. İki adımlı publish + zip denendi: arşiv kökünde `Ghostlist.exe`,
`cli\ghostlist.exe`; self-contained tek dosya GUI açıldı.

## Çıktı
**Yeni dosyalar.** `Ghostlist.Cli/{Ghostlist.Cli.csproj,CommandLine.cs,Reporter.cs,Program.cs}`,
`Ghostlist.Core/SystemRestore.cs`, `.github/workflows/{ci,release}.yml`,
`Ghostlist.Tests/CliTests/CommandLineTests.cs` (21 test).

**owns genişletildi** (YURUTME adım 2, LOG'a da yazıldı):
`Ghostlist.Tests/Ghostlist.Tests.csproj` (CLI'ya proje başvurusu — testler onsuz derlenmez),
`Ghostlist.App/ViewModels/MainViewModel.cs` (T5 dosyası; kriter 3 "GUI ve CLI" dediği için
toplu düzeltmeden önce `SystemRestore.TryCreate` çağrısı eklendi, tek satır),
`.gitignore` (`publish/` ve release zip'i — aksi halde `git add -A` derleme çıktısını yutuyor).

**Ad çakışması kararı.** GUI derlemesi `Ghostlist.exe`, CLI derlemesi `ghostlist` —
Windows'ta aynı klasörde çakışırlar. Sözleşme komutları `ghostlist scan` diye yazdığı için
CLI adı korundu ve arşivde `cli\` alt klasörüne konuldu; o klasör PATH'e girince komut
sözleşmedeki gibi yazılıyor. `scripts/install.ps1`'in beklediği `Ghostlist-win-x64.zip`
adı ve kökteki `Ghostlist.exe` **değişmedi**, kurulum kırılmıyor.

**Dar yorum seçimleri.**
- `scan` yalnızca `Broken` ve `Suspicious` bulguları listeler; `Healthy` kaldırma kayıtları
  (bu makinede 456 tane) ve `Unsupported` sistem bileşenleri çıktıya girmez, yoksa çıkış kodu
  hep 1 olurdu. Çıkış: listelenecek bulgu varsa 1, yoksa 0.
- `fix --all` `CleanupService.AutoFixable` üzerinden gider, yani eşik ve kategori kısıtı
  T3'ün `ConfidenceRules`'undan gelir; CLI kendi kopyasını tutmuyor. `--min-confidence`
  yalnızca bunu **daraltabilir**, gevşetemez (varsayılan 90).
- `--dry-run` sistem geri yükleme noktası bile oluşturmaz, hiçbir yan etkisi yok, çıkış 1.
- `--min-confidence` tek bulgu düzeltmede anlamsız olduğu için ayrıştırma hatası.
- Onay istemi yalnızca terminalde; `Console.IsInputRedirected` ise çıkış 2 — sözleşmenin
  "boru hattında çalışmaz" maddesi.

**CI notu.** `release.yml` imzalama içermez, SHA256 dosyası yayımlar. İki publish adımı
arka arkaya yerel makinede bir kez geçici MSB4018 ile düştü (dosya kilidi), temiz dizinde
tekrarlanmadı; CI her koşuda temiz runner kullandığı için ek önlem alınmadı.

**Kapsam dışı not:** `Ghostlist.Core` hâlâ `net8.0-windows`; CLI de öyle olmak zorunda kaldı.
Core'u `net8.0`'a indirip CLI'yi platformdan bağımsız kılmak ayrı bir tur işi.
